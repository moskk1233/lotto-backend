import { type Config } from 'prettier';

const config: Config = {
  tabWidth: 2,
  useTabs: false,
  printWidth: 100,
  arrowParens: 'always',
  endOfLine: 'lf',
  semi: true,
  trailingComma: 'all',
  singleQuote: true,
};

export default config;
